const { MainDispatcher } = require("./mainDispatcher");
// import {interruptionDispatcher} from "./interruptionDispatcher";

module.exports = {
    MainDispatcher: MainDispatcher,
    // InterruptionDispatcher: interruptionDispatcher
}
